# 2026-07-02 工作日志

## GitHub Actions Android APK 构建配置

为 Markor 项目配置了完整的 GitHub Actions CI 自动构建流程。

### 所做工作
1. **分析项目构建配置**：
   - AGP 8.13.2, Gradle 8.14.5, compileSdk 35
   - 三种 Product Flavor（flavorAtest/flavorDefault/flavorGplay）
   - 已配置阿里云镜像源
   - build.gradle 中使用了 git rev-parse/git log 命令
   - 已有完整 gradle/wrapper 目录

2. **创建 `.github/workflows/build.yml`**：
   - 基于 `actions/checkout@v4` + `setup-java@v4` (JDK 17) + `android-actions/setup-android@v3`
   - 构建命令：`./gradlew assembleFlavorDefaultDebug`
   - 上传 Artifacts（保留 30 天）
   - 触发方式：push 任意分支 + workflow_dispatch 手动触发

3. **用户指导**：
   - 从 git init 到推送的完整命令
   - gradle/wrapper 必须上传的原因说明
   - 查看构建进度、下载 APK 的指引
   - main vs master 分支名避坑
   - 首次构建耗时预期
